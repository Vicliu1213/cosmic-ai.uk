# Cosmic AI 自定义配置示例

system:
  name: Custom Cosmic AI
  environment: staging

engine:
  type: quantum
  enabled: true

database:
  type: postgresql
  host: staging-db.example.com

api:
  port: 443
  authentication: oauth2

trading:
  symbol: ETH/USDT
  position_size: 0.5
