# Fractal Recursion

    Hermes local module surface for `fractal_recursion`.

    Source of truth:
    - core entry: `core.py`
    - dashboard catalog: `hermes/dashboard/module_catalog.json`
    - canonical config: `config/fractal_recursion.yaml`

    Components:
    - `fractal_dim`
- `recursive_engine`
- `iterated_function`
- `scale_analyzer`
- `self_similarity`

    Capabilities:
    - `recursion`
- `scaling`
- `similarity`
- `analysis`

    This local README exists so the dashboard catalog has a concrete, inspectable doc target inside `hermes/src/fractal_recursion/`.
